<UserControl x:Class="FPBooster.FPBoosterPlus.CloudAutoBumpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="1200" d:DesignHeight="700"
             Background="Transparent" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="CloudCard" TargetType="Border">
            <Setter Property="Background" Value="#25202B"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderBrush" Value="#443355"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/> <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#AA000000" BlurRadius="20" ShadowDepth="5" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PluginPrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#6A4C93"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="B" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PluginSecondaryButton" TargetType="Button" BasedOn="{StaticResource PluginPrimaryButton}">
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="Foreground" Value="#DDD"/>
        </Style>

        <Style x:Key="CloudSaveButton" TargetType="Button" BasedOn="{StaticResource PluginPrimaryButton}">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#007BFF" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PluginWatermarkTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="#DDD"/>
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="Bd" CornerRadius="8" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                            <TextBlock x:Name="Watermark" Margin="12,0,0,0" Foreground="#888" 
                                       Text="{TemplateBinding Tag}" IsHitTestVisible="False"
                                       VerticalAlignment="Center" Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PluginCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#DDD"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="CheckBorder" Width="22" Height="22" CornerRadius="6" 
                                    BorderBrush="#555" BorderThickness="1.5" Background="#33FFFFFF">
                                <Path x:Name="CheckMark" Data="M2,8 L8,14 L18,4" Stroke="White" StrokeThickness="2.5" 
                                      VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0"/>
                            </Border>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#6A4C93"/>
                                <Setter TargetName="CheckMark" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,25">
            <Button Content="← Назад" Click="OnBackClick" Width="100" Height="30"
                    Style="{StaticResource PluginSecondaryButton}" DockPanel.Dock="Left" Margin="0,0,20,0"/>
            
            <TextBlock Text="☁️ AutoBump Настройки" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                <Button Content="🔄 Обновить статус" Click="OnRefreshClick" Style="{StaticResource PluginSecondaryButton}" Width="140" Height="30"/>
            </StackPanel>
        </DockPanel>

        <UniformGrid Grid.Row="1" Columns="3" Rows="1" Margin="0,0,0,25">
            <Border Style="{StaticResource CloudCard}" Margin="0,0,10,0">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="СТАТУС ЗАДАЧИ" Foreground="#888" FontSize="11" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtStatus" Text="Загрузка..." Foreground="#00FF99" FontSize="16" FontWeight="SemiBold" Margin="0,5,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Style="{StaticResource CloudCard}" Margin="5,0,5,0">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="СЛЕДУЮЩИЙ ЗАПУСК" Foreground="#888" FontSize="11" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtNextRun" Text="—" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Style="{StaticResource CloudCard}" Margin="10,0,0,0" Background="#3A2A4D" BorderBrush="#9B4BFF">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="АКТИВНОСТЬ" Foreground="#D6BCFA" FontSize="11" FontWeight="Bold"/>
                        <CheckBox x:Name="SwitchActive" Content="Включено на сервере" Style="{StaticResource PluginCheckBox}" FontSize="16" FontWeight="Bold" Margin="0,10,0,0"
                                  Checked="OnSwitchToggled" Unchecked="OnSwitchToggled"/>
                    </StackPanel>
                </Grid>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CloudCard}" Margin="0,0,15,0">
                <ScrollViewer>
                    <StackPanel> 
                        <TextBlock Text="Node ID (Лоты)" Foreground="#AAA" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="InputNodes" Style="{StaticResource PluginWatermarkTextBox}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Tag="ID лотов..."/>
                        <Button Content="📥 Импорт из лаунчера" Margin="0,5,0,20" Style="{StaticResource PluginSecondaryButton}" Height="30" Click="OnImportNodesClick"/>

                        <TextBlock Text="Golden Key" Foreground="#AAA" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="InputKey" Style="{StaticResource PluginWatermarkTextBox}" IsReadOnly="True" Opacity="0.7"/>
                        <Button Content="🔑 Импорт ключа" Margin="0,5,0,20" Style="{StaticResource PluginSecondaryButton}" Height="30" Click="OnImportKeyClick"/>

                        <Button x:Name="BtnSave" Content="💾 СОХРАНИТЬ НА СЕРВЕРЕ" Style="{StaticResource CloudSaveButton}" Height="50" Click="OnSaveClick"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CloudCard}" Background="#111">
                <ListBox x:Name="LogList" Background="Transparent" BorderThickness="0" Foreground="#DDD" Padding="10">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}" Foreground="{Binding Color}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>
    </Grid>
</UserControl>