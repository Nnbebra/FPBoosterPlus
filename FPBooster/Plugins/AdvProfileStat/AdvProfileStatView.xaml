<UserControl x:Class="FPBooster.Plugins.AdvProfileStatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="600"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="PluginCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Background" Value="{DynamicResource BrushPanel}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Color="#44000000" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PluginButton" TargetType="Button">
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="650"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource PluginCard}" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📊 Статистика профиля" Foreground="{DynamicResource BrushAccentLight}" FontSize="20" FontWeight="Bold"/>
                        <TextBlock x:Name="LastUpdateText" Text="Ожидание..." Foreground="{DynamicResource BrushSubText}" FontSize="10"/>
                    </StackPanel>
                    <TextBlock x:Name="PluginStatus" Text="Wait" Foreground="Gray" DockPanel.Dock="Right" VerticalAlignment="Top"/>
                </DockPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Background="{DynamicResource BrushPanelAlt}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="Общий баланс (RUB)" Foreground="Gray" FontSize="10"/>
                                        <TextBlock x:Name="NowValue" Text="0 ₽" Foreground="White" FontSize="26" FontWeight="Black"/>
                                    </StackPanel>
                                    <Button x:Name="QuickRefreshButton" Content="🔄" HorizontalAlignment="Right" VerticalAlignment="Top" 
                                            Width="30" Height="30" Click="QuickRefreshButton_Click" Style="{StaticResource PluginButton}"/>
                                </Grid>
                                <UniformGrid Columns="3" Margin="0,10,0,0">
                                    <StackPanel>
                                        <TextBlock Text="RUB" Foreground="Gray" FontSize="9"/>
                                        <TextBlock x:Name="RubValue" Text="0 ₽" Foreground="LightGreen" FontWeight="Bold"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="USD" Foreground="Gray" FontSize="9"/>
                                        <TextBlock x:Name="UsdValue" Text="0 $" Foreground="LightBlue" FontWeight="Bold"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="EUR" Foreground="Gray" FontSize="9"/>
                                        <TextBlock x:Name="EurValue" Text="0 €" Foreground="LightYellow" FontWeight="Bold"/>
                                    </StackPanel>
                                </UniformGrid>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource BrushPanelAlt}" CornerRadius="8" Padding="15">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="Продажи" FontWeight="Bold" Foreground="White"/>
                                    <Button x:Name="RefreshButton" Content="🔄 Обновить всё" DockPanel.Dock="Right" Click="RefreshButton_Click" Style="{StaticResource PluginButton}"/>
                                </DockPanel>
                                
                                <Grid x:Name="StatsGrid">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource PluginCard}" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Margin="0,0,0,10">
                    <TextBlock Text="📋 Лог" Foreground="White" FontWeight="Bold"/>
                    <TextBlock x:Name="LogCounter" Text="0" Foreground="Gray" DockPanel.Dock="Right" Margin="5,0"/>
                    <Button Content="Очистить" DockPanel.Dock="Right" Click="OnClearPluginLog" Style="{StaticResource PluginButton}"/>
                </DockPanel>
                
                <ListBox x:Name="PluginLog" Grid.Row="1" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}" Foreground="{Binding Color}" TextWrapping="Wrap" FontSize="11"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>