<UserControl x:Class="FPBooster.Plugins.AutoBumpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="900" d:DesignHeight="600"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="GlowingCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Background" Value="#660A0A10"/> 
            <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="35" ShadowDepth="0" Opacity="0.5" 
                                      Color="{DynamicResource ColorAccentDeep}"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PluginHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource BrushAccentLight}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Black"/>
        </Style>

        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource BrushSubText}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Hidden" Margin="0,0,15,0">
            <StackPanel>
                
                <Border Style="{StaticResource GlowingCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="🔄" HorizontalAlignment="Center" FontSize="20" Margin="0,0,0,5"/>
                            <TextBlock x:Name="TxtTotal" Text="0" Style="{StaticResource StatValue}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Попыток" Style="{StaticResource StatLabel}" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <Border Grid.Column="1" Width="1" Background="{DynamicResource BrushBorder}" Margin="10,5"/>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock Text="✅" HorizontalAlignment="Center" FontSize="20" Margin="0,0,0,5"/>
                            <TextBlock x:Name="TxtSuccess" Text="0" Style="{StaticResource StatValue}" HorizontalAlignment="Center" Foreground="{DynamicResource BrushAccentLight}"/>
                            <TextBlock Text="Поднято" Style="{StaticResource StatLabel}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource GlowingCard}">
                    <StackPanel>
                        <TextBlock Text="НАСТРОЙКИ ПОДНЯТИЯ" Style="{StaticResource PluginHeader}"/>
                        
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Интервал (часов)" Foreground="{DynamicResource BrushSubText}" FontSize="11" Margin="0,0,0,4"/>
                                <TextBox x:Name="IntervalInput" Text="4" Style="{StaticResource WatermarkTextBox}" TextAlignment="Center"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Разброс (мин)" Foreground="{DynamicResource BrushSubText}" FontSize="11" Margin="0,0,0,4"/>
                                <TextBox x:Name="JitterInput" Text="15" Style="{StaticResource WatermarkTextBox}" TextAlignment="Center" ToolTip="Случайная задержка +/- минут"/>
                            </StackPanel>
                        </Grid>

                        <Border Background="#22000000" CornerRadius="8" Padding="12" Margin="0,0,0,15">
                            <DockPanel>
                                <TextBlock Text="Статус:" Foreground="{DynamicResource BrushSubText}"/>
                                <TextBlock x:Name="StatusText" Text="ГОТОВ" FontWeight="Bold" HorizontalAlignment="Right" Foreground="White">
                                    <TextBlock.Effect>
                                        <DropShadowEffect x:Name="StatusGlow" BlurRadius="8" Color="White" Opacity="0"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </DockPanel>
                        </Border>

                        <Button x:Name="BtnStart" Content="▶ ЗАПУСТИТЬ АВТО" Click="OnStartAutoClick" Style="{StaticResource PrimaryButton}" Height="42" Margin="0,0,0,8"/>
                        <Button x:Name="BtnStop" Content="⏹ ОСТАНОВИТЬ" Click="OnStopAutoClick" Style="{StaticResource SecondaryButton}" Height="42" Visibility="Collapsed" Margin="0,0,0,8"/>
                        
                        <Button Content="⚡ ПОДНЯТЬ СЕЙЧАС (1 КРУГ)" Click="OnQuickBumpClick" Style="{StaticResource SecondaryButton}" Height="34"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GlowingCard}" BorderBrush="{DynamicResource BrushAccentDeep}" BorderThickness="1">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,10">
                            <TextBlock Text="🍃 ECO MODE" Style="{StaticResource PluginHeader}" Foreground="#9CF2B2" VerticalAlignment="Center" Margin="0"/>
                        </DockPanel>
                        <TextBlock Text="Сверните программу в трей для максимальной производительности." 
                                   Foreground="{DynamicResource BrushSubText}" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,12"/>
                        <Button Content="Включить Eco Mode" Click="OnEcoModeFromPlugin" 
                                Style="{StaticResource SecondaryButton}" 
                                BorderBrush="#449CF2B2" Foreground="#9CF2B2" Background="#119CF2B2"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" Style="{StaticResource GlowingCard}" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <DockPanel Grid.Row="0" Margin="0,0,0,10" LastChildFill="False">
                    <TextBlock Text="ЖУРНАЛ ОПЕРАЦИЙ" Style="{StaticResource PluginHeader}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    <Button Content="Очистить" Width="80" Height="24" DockPanel.Dock="Right" Click="OnClearPluginLog" Style="{StaticResource SecondaryButton}" FontSize="11"/>
                </DockPanel>
                
                <Border Grid.Row="1" Height="1" Background="{DynamicResource BrushBorder}" Margin="0,0,0,10" Opacity="0.5"/>
                
                <ListBox Grid.Row="2" x:Name="PluginLog" Style="{StaticResource ModernListBox}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}" Foreground="{Binding Color}" TextWrapping="Wrap" FontSize="12" FontFamily="Consolas" Margin="0,2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>