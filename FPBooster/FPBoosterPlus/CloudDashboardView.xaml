<UserControl x:Class="FPBooster.FPBoosterPlus.CloudDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="PluginPrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#6A4C93"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PluginSecondaryButton" TargetType="Button" BasedOn="{StaticResource PluginPrimaryButton}">
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="Foreground" Value="#DDD"/>
        </Style>

        <Style x:Key="MenuCard" TargetType="Button">
            <Setter Property="Background" Value="#25202B"/>
            <Setter Property="BorderBrush" Value="#443355"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="160"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="16">
                            <Grid Margin="20">
                                <StackPanel VerticalAlignment="Bottom">
                                    <TextBlock Text="{TemplateBinding Tag}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{TemplateBinding Content}" Foreground="#888" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                </StackPanel>
                                <TextBlock Text="☁" FontSize="32" Foreground="#33FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2D2636"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#9B4BFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="FPBooster" FontSize="32" FontWeight="Black" Foreground="White"/>
                <TextBlock Text="PLUS" FontSize="32" FontWeight="Light" Foreground="#D6BCFA" Margin="10,0,0,0"/>
            </StackPanel>
            <Border DockPanel.Dock="Right" Background="#33FFFFFF" CornerRadius="20" Padding="15,5" VerticalAlignment="Center">
                <TextBlock x:Name="ServerStatusText" Text="🟢 Сервер онлайн" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
            </Border>
        </DockPanel>

        <Border Grid.Row="1" Background="#1A1A20" CornerRadius="16" Padding="25" Margin="0,0,0,30">
            <DockPanel>
                <StackPanel>
                    <TextBlock Text="DASHBOARD" Foreground="#AAA" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="Панель управления сервером" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,5,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <TextBlock Text="МОДУЛИ" VerticalAlignment="Top" Foreground="#666" FontWeight="Bold" Margin="5,0,0,15"/>
            
            <UniformGrid Columns="3" Rows="2" Margin="0,30,0,0">
                <Button Style="{StaticResource MenuCard}" 
                        Tag="Auto Bump" 
                        Content="Автоматическое поднятие лотов 24/7 без участия вашего ПК."
                        Click="OpenAutoBump_Click" Margin="0,0,15,15"/>

                <Button Style="{StaticResource MenuCard}" 
                        Tag="Auto Restock" 
                        Content="Автовыдача товаров (Скоро)."
                        IsEnabled="False" Opacity="0.5" Margin="0,0,15,15"/>

                <Button Style="{StaticResource MenuCard}" 
                        Tag="Analytics" 
                        Content="Подробная статистика продаж (Скоро)."
                        IsEnabled="False" Opacity="0.5" Margin="0,0,0,15"/>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>