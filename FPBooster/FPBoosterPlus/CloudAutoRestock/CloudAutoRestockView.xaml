<UserControl x:Class="FPBooster.FPBoosterPlus.CloudAutoRestockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    
    <UserControl.Resources>
        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="#22151520"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <Button x:Name="BtnBack" Content="⬅ Назад" Click="OnBackClick" Background="Transparent" Foreground="Gray" BorderThickness="0"/>
            <TextBlock Text="Cloud Auto Restock" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource GlassPanel}" Padding="20" Margin="0,0,20,0">
                <StackPanel>
                    <TextBlock Text="Добавление товара" Foreground="White" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="ID Лота (Node ID):" Foreground="Gray"/>
                    <TextBox x:Name="InputNodeId" Height="30" Margin="0,5,0,10" Background="#22FFFFFF" Foreground="White"/>
                    
                    <TextBlock Text="Мин. остаток (Лимит):" Foreground="Gray"/>
                    <TextBox x:Name="InputLimit" Height="30" Margin="0,5,0,10" Text="10" Background="#22FFFFFF" Foreground="White"/>
                    
                    <TextBlock Text="Добавить ключи (каждый с новой строки):" Foreground="Gray"/>
                    <TextBox x:Name="InputKeys" Height="150" AcceptsReturn="True" Margin="0,5,0,10" Background="#22FFFFFF" Foreground="White" 
                             ToolTip="Эти ключи будут отправлены на сервер и сохранены в БД"/>
                    
                    <Button Content="➕ Добавить в конфиг" Height="35" Click="OnAddLotClick" Background="#44FFFFFF" Foreground="White" Margin="0,0,0,20"/>
                    
                    <Separator Opacity="0.2" Margin="0,0,0,20"/>
                    
                    <CheckBox x:Name="SwitchActive" Content="Плагин Активен" Foreground="White" FontSize="16"/>
                    <Button x:Name="BtnSaveServer" Content="💾 Сохранить всё на сервере" Height="45" Click="OnSaveServerClick" 
                            Background="#007ACC" Foreground="White" Margin="0,15,0,0" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource GlassPanel}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <TextBlock Text="Активные задачи" Foreground="Gray" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TxtStatus" Text="Статус: Загрузка..." DockPanel.Dock="Right" Foreground="Orange" HorizontalAlignment="Right"/>

                    <ListBox x:Name="ListLots" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="White">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#11FFFFFF" CornerRadius="5" Padding="10" Margin="0,5">
                                    <DockPanel>
                                        <Button Content="🗑" Click="OnDeleteLotClick" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Foreground="Red"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding NodeId, StringFormat='Лот: {0}'}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding MinQty, StringFormat='Пополнять если меньше: {0} шт.'}" Foreground="Gray" FontSize="12"/>
                                            <TextBlock Text="{Binding KeysInfo}" Foreground="LightGreen" FontSize="12"/>
                                        </StackPanel>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>