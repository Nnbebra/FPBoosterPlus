<UserControl x:Class="FPBooster.FPBoosterPlus.CloudAutoRestockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="750" Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="#22151520"/><Setter Property="CornerRadius" Value="16"/><Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/><Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect"><Setter.Value><DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.2"/></Setter.Value></Setter>
        </Style>
        <Style x:Key="GlassTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="#15FFFFFF"/><Setter Property="BorderBrush" Value="#33FFFFFF"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TextBox"><Border x:Name="bd" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"><ScrollViewer x:Name="PART_ContentHost" Margin="2"/></Border></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="GlassPrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="bd" CornerRadius="8" Background="{DynamicResource BrushAccentDeep}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15,8"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Opacity" Value="0.8"/></Trigger><Trigger Property="IsEnabled" Value="False"><Setter TargetName="bd" Property="Opacity" Value="0.4"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40,30">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,30">
            <Button x:Name="BtnBack" Click="OnBackClick" Content="⬅ Назад" Background="Transparent" Foreground="Gray" BorderThickness="0" DockPanel.Dock="Left"/>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <TextBlock Text="📦" FontSize="26" Margin="0,0,10,0"/><TextBlock Text="Auto Restock Cloud" FontSize="26" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="400"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,30,0">
                <Border Style="{StaticResource GlassPanel}" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="ДОСТУП И ИМПОРТ" Foreground="#888" FontSize="11" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBlock Text="Golden Key" Foreground="Gray" FontSize="11" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="InputKey" Height="35" Background="#15FFFFFF" Foreground="White" Margin="0,0,0,10"/>
                        <DockPanel Margin="0,5,0,5"><TextBlock Text="Node IDs" Foreground="Gray" FontSize="11"/><Button Content="📥 Импорт" Click="OnImportClick" Background="Transparent" Foreground="#00FF99" BorderThickness="0" DockPanel.Dock="Right" Cursor="Hand"/></DockPanel>
                        <TextBox x:Name="InputNodes" Style="{StaticResource GlassTextBox}" Height="100" AcceptsReturn="True" Margin="0,0,0,15" Tag="Введите Node ID через запятую или строку"/>
                        
                        <Button x:Name="BtnLoadOffers" Content="🔍 ЗАГРУЗИТЬ ОФФЕРЫ" Click="OnLoadOffersClick" Style="{StaticResource GlassPrimaryButton}" Height="40"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource GlassPanel}" Padding="15" VerticalAlignment="Stretch">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <DockPanel Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock Text="ЖУРНАЛ СОБЫТИЙ" Foreground="#888" FontSize="11" FontWeight="Bold"/>
                            <Button Content="Очистить" Click="OnClearLogClick" Background="Transparent" Foreground="Gray" BorderThickness="0" DockPanel.Dock="Right"/>
                        </DockPanel>
                        <ListBox x:Name="LogList" Grid.Row="1" Background="Transparent" BorderThickness="0" Height="250">
                            <ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding Text}" Foreground="{Binding Color}" FontSize="11" TextWrapping="Wrap" Margin="0,2"/></DataTemplate></ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </StackPanel>

            <Border Grid.Column="1" Style="{StaticResource GlassPanel}" Padding="20">
                <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="НАСТРОЙКА ТОВАРОВ" Foreground="#888" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                            <TextBlock x:Name="TxtStatus" Text="Готов" Foreground="Gray" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <CheckBox x:Name="SwitchActive" Content="АКТИВНО" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DockPanel>
                    <ListBox x:Name="ListOffers" Grid.Row="1" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate><DataTemplate>
                            <Border Background="#10FFFFFF" CornerRadius="10" Padding="15" Margin="0,0,0,10">
                                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"><TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold"/><TextBlock Text="{Binding StatusInfo}" Foreground="Gray" FontSize="11"/></StackPanel>
                                    <StackPanel Grid.Column="1" Margin="10,0"><TextBlock Text="Мин. кол-во" Foreground="Gray" FontSize="10"/><TextBox Text="{Binding MinQty}" Style="{StaticResource GlassTextBox}" Height="30"/></StackPanel>
                                    <StackPanel Grid.Column="2" Width="200"><TextBlock Text="Добавить ключи" Foreground="Gray" FontSize="10"/><TextBox Text="{Binding KeysToAddRaw}" Style="{StaticResource GlassTextBox}" Height="50" AcceptsReturn="True"/></StackPanel>
                                    <Button Grid.Column="2" VerticalAlignment="Top" HorizontalAlignment="Right" Content="✕" Click="OnDeleteOfferClick" Background="Transparent" Foreground="Red" BorderThickness="0" Margin="0,-10,-10,0"/>
                                </Grid></Border>
                        </DataTemplate></ListBox.ItemTemplate>
                    </ListBox>
                    <Button x:Name="BtnSave" Grid.Row="2" Content="💾 СОХРАНИТЬ КОНФИГУРАЦИЮ" Click="OnSaveClick" Style="{StaticResource GlassPrimaryButton}" Height="45" Margin="0,15,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>