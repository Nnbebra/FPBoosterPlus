<Window x:Class="FPBooster.UI.PluginsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Доступные плагины"
        Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        Opacity="0">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" CornerRadius="10" Background="#339D4EDD" BorderBrush="#559D4EDD" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Background" Value="#559D4EDD"/>
                                <Setter TargetName="B" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#9D4EDD" BlurRadius="12" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="B" Property="Opacity" Value="0.3"/>
                                <Setter TargetName="B" Property="Background" Value="#11FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" BorderThickness="1" BorderBrush="#33FFFFFF">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#CC121218" Offset="0"/>
                <GradientStop Color="#EE0A0A10" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="Black" Opacity="0.7"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
                <TextBlock Text="🧩" FontSize="24" Margin="0,0,10,0"/>
                <TextBlock Text="КАТАЛОГ ПЛАГИНОВ" Foreground="#E0AAFF" FontSize="20" FontWeight="Black" VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#E0AAFF" BlurRadius="15" Opacity="0.4"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="2" Rows="3">
                <Button Content="АВТО ПОДНЯТИЕ" Click="OpenAutoBump_Click"/>
                <Button Content="АКТИВАЦИЯ ЛОТОВ" Click="OpenLotsToggle_Click"/>
                
                <Button Content="УДАЛЕНИЕ ЛОТОВ" Click="OpenLotsDelete_Click">
                    <Button.Background>
                        <SolidColorBrush Color="#33FF4B4B"/> </Button.Background>
                </Button>
                
                <Button Content="АВТОПОПОЛНЕНИЕ" Click="OpenAutoRestock_Click"/>
                <Button Content="СТАТИСТИКА ПРОФИЛЯ" Click="OpenAdvProfileStat_Click"/>
                
                <Button Content="СКОРО..." IsEnabled="False" Foreground="#88FFFFFF"/>
            </UniformGrid>

            <Button Grid.Row="2" Content="ЗАКРЫТЬ" Width="150" Height="40" Click="OnOkClick" Margin="0,20,0,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" CornerRadius="20" Background="Transparent" BorderBrush="#44FFFFFF" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>